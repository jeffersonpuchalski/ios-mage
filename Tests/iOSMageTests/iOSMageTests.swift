import XCTest
@testable import iOSMage

final class ios_mageTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        //XCTAssertEqual(ios_mage().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
